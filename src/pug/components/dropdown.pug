mixin dropdown(text,buttons,id,data_atr)
  .select(id=id)
    .select__input(data-type=data_atr)
      p=text
      include ./arrow.pug
      +arrow()
    .select__dropdown
      ul(class="list_categories")
      if buttons===true       
        .select__button
          button(id="button_clear") ОЧИСТИТЬ
          button ПРИМЕНИТЬ
      else
      
